# Stack per DISPATCH_BUILD_BLUEPRINT_v1 Part 3.1: Python 3.11+, SQLite (stdlib), no
# cloud dependencies for storage or state.
jsonschema>=4.20  # dev/test tooling only (Lane A)
pytest>=8.0       # dev/test tooling only
flask>=3.0        # Lane B's queue UI (and, later, Lane D's Reports UI)

# Lane C's vision extractor (src/dispatch/receipt/extraction/vision.py)
# still lazily imports anthropic so a missing key/package degrades to
# VisionExtractionUnavailable rather than an ImportError. Uncommented
# 2026-08-05: Mike supplied a real API key and this lane's extraction
# was exercised live against a real receipt image (see
# docs/lanes/C/NOTES.md, Session 3). No automated test in this build
# calls the real API -- that stays a live, human-run check, not
# something the deterministic suite depends on.
anthropic>=0.40
