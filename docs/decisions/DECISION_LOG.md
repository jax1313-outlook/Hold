# DECISION_LOG

Seeded 2026-08-04 with four open entries, per `DISPATCH_HOLD_SEED_PACKAGE_v1`
§2. Each entry gets its own file here (`DECISION_<n>_<slug>.md`) once
resolved. No open session ever absorbs a decision mid-build — sessions end,
follow-on packets begin, per the Execution Package's Hold Re-Entry
Protocol.

## Open

### #2 — Dual-record fuel (D1 cross-link field)

- **Status:** OPEN, held since 2026-08-03.
- **Blocks:** `fuel_record` schema freeze, the Lane C router, Lane D's
  fuel fixture cross-link assumptions.
- **On approval:** issue Packet C-2 (Router & Freeze) per the Execution
  Package's Hold Re-Entry Protocol.

### #3 — Closed expense vocabulary

- **Status:** OPEN, held since 2026-08-03.
- **Blocks:** `expense_record` category validation, the Lane C router,
  Lane D's Expense Summary template/filter/fixtures.
- **On approval:** vocabulary ships as versioned Library data
  (`library_seed/Vocabulary/`); Packet C-2 opens; Lane D's Expense Summary
  unlocks as a template + fixture (no code change, if Lane D was built
  right).

### #4 — Hard approval gates

- **Status:** OPEN, held since 2026-08-03.
- **Blocks:** the Base Constitution's "HARD APPROVAL GATES" section
  (reserved, empty, in `docs/governance/DISPATCH_BASE_CONSTITUTION_v1.md`);
  the first merge into `integration` (docs-match-as-built gate). Does not
  block any lane's mechanical build.
- **On approval:** document work only — adopt the amendment, stamp the
  version, first `integration` merge is cleared.

### #14 — Trade Memory doctrine

- **Status:** OPEN, held since 2026-08-03.
- **Blocks:** the Trade Memory section of
  `docs/governance/MEMORY_DOCTRINE_v1.md` (reserved, empty); any pattern
  storage or reliance anywhere in Group 1.
- **On approval:** issue Packet C-3 (Trade Memory) before any journeyman
  certification, seeded from human rulings already accumulated in the
  audit trail.

## Resolved

(none yet)
